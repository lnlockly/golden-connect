# Postgres backup CronJob — every 6 hours.
# Dumps trendex DB, gzips (~4.5MB), sends to Telegram channel via Trendex_bizbot,
# and keeps last 12 dumps (3 days) on a small PVC as local fallback.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trendex-pg-backups
  namespace: trendex
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: Secret
metadata:
  name: trendex-pg-backup-secret
  namespace: trendex
type: Opaque
stringData:
  PGPASSWORD: "f055b85bb624d055f4b8d2c3439e883f5f4b7ff7e337347e"
  BOT_TOKEN: "8716444883:AAEbCxOVssb53snq3SkgluqI_K_zpl9Wc88"
  CHAT_ID: "-1003992780880"
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: trendex-pg-backup
  namespace: trendex
spec:
  schedule: "0 */6 * * *"     # every 6 hours
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: backup
              image: postgres:16-alpine
              envFrom:
                - secretRef: { name: trendex-pg-backup-secret }
              volumeMounts:
                - name: backups
                  mountPath: /backups
              command:
                - /bin/sh
                - -c
                - |
                  set -e
                  apk add --no-cache curl >/dev/null 2>&1
                  TS=$(date +%Y%m%d-%H%M%S)
                  FILE="/backups/trendex-${TS}.sql.gz"
                  echo "[backup] dumping → $FILE"
                  pg_dump -h trendex-postgres.trendex.svc.cluster.local -U trendex_owner -d trendex \
                    --schema=public --schema=roboai --schema=eliza_runtime --schema=drizzle \
                    | gzip > "$FILE"
                  SIZE=$(stat -c%s "$FILE")
                  echo "[backup] size: $SIZE bytes"
                  # Send to Telegram
                  CAP="🗄 Trendex DB backup ${TS} ($(echo $SIZE | awk '{printf "%.1f MB", $1/1048576}'))"
                  RESP=$(curl -sS -F "chat_id=${CHAT_ID}" -F "document=@${FILE}" -F "caption=${CAP}" \
                    "https://api.telegram.org/bot${BOT_TOKEN}/sendDocument" || true)
                  echo "[backup] telegram: $RESP"
                  # Rotate: keep last 12
                  ls -1t /backups/trendex-*.sql.gz | tail -n +13 | xargs -r rm -f
                  echo "[backup] done. kept: $(ls -1 /backups/trendex-*.sql.gz | wc -l) dumps"
              resources:
                requests: { cpu: "50m", memory: "64Mi" }
                limits:   { cpu: "500m", memory: "256Mi" }
          volumes:
            - name: backups
              persistentVolumeClaim:
                claimName: trendex-pg-backups
