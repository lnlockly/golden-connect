---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: golden-connect-bot
  namespace: golden-connect
  labels:
    app: golden-connect-bot
spec:
  replicas: 1
  strategy:
    type: Recreate  # long-poller — only one process should own the update stream
  selector:
    matchLabels:
      app: golden-connect-bot
  template:
    metadata:
      labels:
        app: golden-connect-bot
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: golden-connect-bot
          image: ghcr.io/lnlockly/golden-connect-bot:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - secretRef:
                name: golden-connect-bot-env
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
          resources:
            requests:
              cpu: 1m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: golden-connect-bot
  namespace: golden-connect
spec:
  selector:
    app: golden-connect-bot
  ports:
    - name: http
      port: 8080
      targetPort: http
